/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if (ds_exists(gridItems, ds_type_grid) && ds_exists(gridItems2, ds_type_grid)) {
	ds_grid_destroy(gridItems);
	ds_grid_destroy(gridItems2);
}








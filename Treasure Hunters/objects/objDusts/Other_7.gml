/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if (sprite_index == sprJumpDust) {
	image_index = image_number -1;
	instance_destroy()
}

if (sprite_index == sprFallDust) {
	image_index = image_number -1;
	instance_destroy()
}

if (sprite_index == sprRunDust) {
	image_index = image_number -1;
	instance_destroy();
}







